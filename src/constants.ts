const CLIENT_ID = 'test';
const STATE = 'abc123';
const REDIRECT_URI = 'http://localhost:5173';
// TODO: Change this to the actual URL once cors issue is resolved
const QACCOUNT_URL = 'http://localhost:4000'; // "https://account.qvantum.com";

export const constants = {
  CLIENT_ID,
  STATE,
  REDIRECT_URI,
  QACCOUNT_URL,
};
